
<div class="container">
    <div class="row row-offcanvas row-offcanvas-right">
        <div class="col-xs-12 col-sm-9">
            <h2>JARVIS</h2>
            <p>Just a rather very intelligent system</p>
            <div>
                <p>
                    Server monitoring software is designed to make sure that a server is active, healthy, and responding to requests appropriately.
                </p>
                <p>
                    JARVIS is advanced server monitoring software which lets you check both basic server uptime and detailed usage statistics on all types of servers, including:
                <ul>
                    <li>web servers</li>
                    <li>databases</li>
                    <li>mail servers</li>
                    <li>hardware resources</li>
                    <li>... and more other servers used in bussines</li>
                </ul>
                </p>
                <div class="col-md-4">
                    <h2> Benefits</h2>
                    <p>text....</p>
                </div>
                <div class="col-md-4">
                    <h2> About</h2>
                    <p>text....</p>
                </div>
                <div class="col-md-4">
                    <h2> Contact Us</h2>
                    <p>text....</p>
                </div>
            </div>
        </div>
        <div class="col-xs-6 col-sm-3 sidebar-offcanvas">
            <div>
                <% if ( flash && flash.err) { %>
                <ul class="alert alert-success">
                    <% Object.keys(flash.err).forEach(function(error) {%>
                    <li><%= JSON.stringify(flash.err[error]) %></li>
                    <% }) %>
                </ul>
                <% } %>
                <form class="form-horizontal" role="form" action="/session/create" method="POST">

                    <div class="form-group">
                        <input class="form-control" type="text" name="email" placeholder="<%= i18n('login').email %>" />
                    </div>
                    <div class="form-group">
                        <input class="form-control" type="password" name="password" placeholder="<%= i18n('login').password %>"/>
                    </div>
                    <div>
                        <button class="btn btn-primary btn-lg" type="submit"><%= i18n('login').login %></button>
                        <a href="#"><%= i18n('login').resetPassword %></a>
                    </div>
                    <input type="hidden" name="_csrf" value="<%= _csrf %>">
                </form>
            </div>
        </div>
    </div>
</div>
